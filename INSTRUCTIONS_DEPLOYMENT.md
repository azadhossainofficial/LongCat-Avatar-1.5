# 🚀 LongCat-Video-Avatar 1.5 Studio — 1-Click Server Deployment Guide

এই ফোল্ডারে আপনার **LongCat-Video-Avatar 1.5 Studio**-এর সমস্ত কাস্টমাইজড ফ্রন্টএন্ড (`web/`), ব্যাকএন্ড (`server.py`), অডিও-ভিডিও লিপ-সিঙ্ক অ্যালগরিদম (`run_demo_avatar_single_audio_to_video.py`), এবং মেমরি প্যাচ সুরক্ষিত আছে।

ভবিষ্যতে যেকোনো নতুন জিপিইউ সার্ভার (Vast.ai, RunPod, Lambda Labs, AWS) নিলে আপনি **মাত্র ১টি কমান্ড দিয়ে পুরো স্টুডিও হুবহু ইনস্টল** করতে পারবেন।

---

## ⚡ পদ্ধতি ১: আপনার ম্যাক থেকেই সরাসরি ১-ক্লিক ডেপ্লয় (Recommended)

আপনার ম্যাকের টার্মিনালে এই ফোল্ডারে এসে শুধু নিচের কমান্ডটি রান করুন এবং আপনার নতুন সার্ভারের SSH কমান্ডটি দিন:

```bash
python3 deploy_master.py --ssh "ssh -p <PORT> root@<IP>"
```

### 🛠️ `deploy_master.py` নিজে নিজে যা যা করবে:
1. নতুন সার্ভারে কানেক্ট করবে।
2. সমস্ত মোবাইল-রেস্পন্সিভ ফ্রন্টএন্ড (`index.html`, `style.css`, `app.js`) আপলোড করবে।
3. সমস্ত ব্যাকএন্ড ও লিপ-সিঙ্ক ইঞ্জিন (`server.py`, `run_demo_avatar_single_audio_to_video.py`) আপলোড করবে।
4. `ffmpeg` এবং `libsndfile` অডিও লাইব্রেরি ইনস্টল করবে।
5. সমস্ত পাইথন ডিপেনডেন্সি ইনস্টল করবে।
6. মডেল ওয়েটস ডাউনলোড বা ভেরিফাই করবে।
7. পোর্ট **20100**-এ লং ক্যাট স্টুডিও স্বয়ংক্রিয়ভাবে ব্যাকগ্রাউন্ডে চালু করে দেবে!

---

## ⚡ পদ্ধতি ২: নতুন সার্ভারের ভেতরে ঢুকে সরাসরি রান করা

যদি আপনি নতুন সার্ভারের ভেতরে SSH দিয়ে ঢুকে থাকেন, তবে সার্ভারের টার্মিনালে শুধু রান করুন:

```bash
bash deploy_new_server.sh
```

---

## 🎯 এআই অ্যাসিস্ট্যান্টকে দেওয়ার জন্য প্রম্পট:

ভবিষ্যতে নতুন সার্ভার নিলে এআই অ্যাসিস্ট্যান্টকে শুধু সার্ভারের আইপি ও পোর্ট দিয়ে বলবেন:
> *"আমার এই ফোল্ডারের `deploy_master.py` ব্যবহার করে আমার নতুন সার্ভারটিতে পুরো LongCat Video Avatar 1.5 Studio হুবহু ইনস্টল ও চালু করে দাও।"*

তাহলে এআই কোনো ফাইল না হারিয়ে ঠিক এই মুহূর্তে যেমন আছে, হুবহু সেইম সিস্টেমে নতুন সার্ভার রেডি করে দেবে!
